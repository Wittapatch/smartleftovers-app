[project]
name = "smartleftovers-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "infisicalsdk>=1.0.16",
    "uuid>=1.30",
]

[tool.uv.workspace]
members = [
    "backend",
]
